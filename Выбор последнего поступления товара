Функция ДатаПоследнегоПоступления(Товар)
    Запрос = Новый Запрос;
    Запрос.Текст = "ВЫБРАТЬ МАКСИМУМ(Док.Дата) КАК ПоследняяДата  
                    ИЗ Документ.ПоступлениеТоваровУслуг КАК Док  
                    ГДЕ Док.Товар = &Товар";  
    Запрос.УстановитьПараметр("Товар", Товар);  
    Результат = Запрос.Выполнить().Выбрать();  
    Если Результат.Следующий() Тогда  
        Возврат Результат.ПоследняяДата;  
    Иначе  
        Возврат "Нет поступлений";  
    КонецЕсли;  
КонецФункции
